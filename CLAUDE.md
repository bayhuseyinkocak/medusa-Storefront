# Claude Code

This project uses **ide-agent-mcp** for cross-IDE agent configuration.

**Read these files first:**
- @AGENTS.md — project overview, commands, skill index
- `ai/rules/` — detailed rules (also in `.agents/rules/`)
- `ai/skills/` — skills (symlinked to `.cursor/skills/`)

Run `ide-agent sync` after editing canonical `ai/` files.
